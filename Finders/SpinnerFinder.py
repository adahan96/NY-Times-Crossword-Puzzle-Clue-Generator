import requests


def findSpinner(word):
    """
    response = requests.get(
        "https://quillbot.com/api/singleParaphrase",
        params={
            "userID": "N/A",
            "text": word,
            "strength": 2,
            "autoflip": True,
            "wikify": True
        }
    )

    json_response = response.json()
    print(json_response)
    """
    return None
